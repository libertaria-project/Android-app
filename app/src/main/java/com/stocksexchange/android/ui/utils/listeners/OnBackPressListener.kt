package com.stocksexchange.android.ui.utils.listeners

/**
 * A listener used for getting notified whenever a user
 * pressed the back button.
 */
interface OnBackPressListener {

    fun onBackPressed()

}